# T4 closing-line — 收束一句话

用途：收束、反问、金句。按分句或关键词渐进出现，不要一开始摊完整句。

字段：

- `base.big`：可选，进入 beat 时显示的底句。
- `step.show.center` / `step.show.big`：随 step 追加的大字内容。

约束：

- 一个 step 通常只追加一句短判断。
- 可用 `<em>` 做朱砂强调。
- 候选最大容量：一页只放 1 个 big item，最多 3 个视觉行。
- 如果收束需要两层意思，优先拆成 T2 过渡 + T4 点睛，不在 T4 里堆两句。
