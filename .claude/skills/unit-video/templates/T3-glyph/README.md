# T3 glyph — 中央大字

用途：题眼、核心字、卷轴揭开后的视觉锤点。

字段：

- `base.glyph`：核心字或短词。
- `steps` 只用于覆盖 atoms，不写 `state/show`。T3 不做渐进解释。

约束：

- `glyph` 最多两行，每行不超过 4 字。
- 不用 T3 放长句。
- 如果需要标题下方解释，用 T2 或新增模板，不要扩展 T3。
- 候选最大容量：只允许 1 个 glyph，总计最多 2 行。
